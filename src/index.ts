/**
 * Entry point — La Guantera
 *
 * Arranca el/los servicios de Fase 1: el bot de Telegram (./consulta/telegram-bot.ts)
 * y, segun como se decida en modo plan, un listener HTTP para recibir el payload
 * reenviado por n8n desde el GitHub Trigger.
 *
 * No implementa logica propia — solo cablea los modulos de ingesta, procesamiento,
 * almacenamiento y consulta ya definidos en sus respectivas carpetas.
 */

export {};
